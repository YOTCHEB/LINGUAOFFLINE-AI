# Audio Dataset Info

This file describes the expected audio dataset structure and naming conventions.

Guidelines:
- Format: 16 kHz WAV
- Duration: 3–6 seconds per sample
- Filenames: `lang_sampleNN.wav` (e.g. `en_sample01.wav`)
- Provide a transcription file per audio clip named `en_sample01.txt` alongside the audio.

Note: audio samples are not included in this starter template.
