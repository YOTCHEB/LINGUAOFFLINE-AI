# Preprocessing Steps (starter)

1. Normalize Unicode and remove control characters.
2. Lowercase or maintain casing consistently depending on model design.
3. Tokenization: use simple whitespace tokenization or a subword tokenizer (SentencePiece/BPE) for production.
4. Remove duplicates and filter offensive content.
5. Save final CSV/TSV files for training.
